EmitSound(
    "garrysmod/save_load1.wav", -- soundName
    Entity(1):GetPos(), -- Position
    1, -- Entity Index
    CHAN_AUTO, -- Channel
    1, -- Volume
    75, -- SND Level
    0, -- soundFlags
    100, -- Pitch
    0, -- DSP Preset
    nil -- Filter
)